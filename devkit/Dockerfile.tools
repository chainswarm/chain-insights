FROM python:3.12-slim

RUN apt-get update \
  && apt-get install -y --no-install-recommends \
    bash \
    ca-certificates \
    default-mysql-client \
    gzip \
  && rm -rf /var/lib/apt/lists/*

RUN pip install --no-cache-dir neo4j==5.28.1

WORKDIR /devkit
