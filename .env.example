# Optional Base RPC override used by wallet balance and the local top-up page.
BASE_RPC_URL=https://base.llamarpc.com

# Optional Chain Insights Graph endpoint override. Local HTTP loopback is allowed.
# Remote Chain Insights Graph endpoints must use https://.
CHAIN_INSIGHTS_GRAPH_MCP_ENDPOINT=http://127.0.0.1:8012/mcp
